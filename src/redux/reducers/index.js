const IninitialState = {
  cart: {
    content: [],
  },
};

const mainReducer = (state = IninitialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
export default mainReducer;
